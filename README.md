# AI POADCAST 全流程指南

本仓库用于将国际教育新闻转化为播客节目，涵盖从原文采集、要点卡片、脚本撰写到音频输出的全链条流程。本文档是唯一的操作手册，其他阶段文件以模板形式存在，可在附录中查阅。若需快速上手，请先阅读 `START_HERE.md`。

---

## 1. 项目目标
- 用标准化流程把英文新闻快速沉淀为中文要点卡片与播客脚本。
- 通过明确的命名规范与索引，保证资料可追溯、可复查。
- 依托 QA 清单与看板，形成持续改进闭环。

---

## 2. 目录结构
```
.
├── README.md                             # 当前文档（单一真相源）
├── Master action plan · MD               # 选题与交付节奏
├── PRD/                                  # 产品方案 & 标准
├── ai_poadcast_main/                     # 主流程脚本与模板
│   ├── import_raw_story.py               # 原文导入 & 存档
│   ├── collect_rss_feeds.py              # RSS 源批量采集
│   ├── batch_extract.py                  # 批量抽取要点摘要（Stage 2 自动化）
│   ├── process_queue.py                  # 队列审核 & 一键导入
│   ├── daily_workflow.py                 # Stage 2/3 每日流水线入口
│   ├── prepare_stage3_prompt.py          # 自动汇总 Stage 3 Prompt
│   ├── generate_stage3_script.py         # 调用 LLM 生成播客脚本
│   ├── qa_checklist_template.md          # QA 质检模板
│   ├── workflow_status_board.md          # 工作流看板示例
│   ├── stage2_outputs/                   # （可选）手工保存的要点卡片草稿
│   ├── stage2_reviews/                   # 术语 / 数字校验记录
│   ├── stage3_inputs/                    # 脚本生成使用的 Prompt
│   └── stage3_reviews/                   # 脚本审核记录
├── podcast-news-aggregator/              # 自动新闻采集 + Demo 脚本
│   ├── scripts/                          # 集成示例（integrated_demo.py 等）
│   ├── config/                           # API 密钥模板
│   └── data/                             # Demo 输出（report_*.md/json）
├── source_archive/                       # 原文档案 + _index.json
├── 要点卡片/YYYY-MM-DD/                    # 卡片终稿（*_card_final.md）
├── 脚本输出/YYYY-MM-DD/                     # 脚本版本（episode_*_v*.md/final）
├── script_chunks/                        # 脚本分段文本（TTS 前处理）
├── audio_parts/                          # TTS 分段音频缓存
├── audio_exports/YYYY/                   # 音频成品（episode_*_<voice>.ext）
├── tts_volcengine.py                     # 火山引擎 TTS 客户端
├── tts_xunfei.py                         # 讯飞 TTS 客户端
├── test_tts.py                           # 火山 TTS 快速测试
├── tts.sh / check_usage.sh               # Shell 工具（批量合成/额度检查）
├── ai_voice_generation_guide (1).md      # 第三方语音解决方案参考
├── volcengine_tts_complete_guide.md      # 火山引擎完整接入指南
├── vendor/                               # 第三方 SDK / Demo
│   └── volcengine_ws_demo/               # WebSocket 协议实现
└── 其它：PRD 文档集、`script.txt`、`.env`、`.venv`、示例音频
```

---

## 3. 流程总览

| 阶段 | 目标 | 输出文件 | 关键工具 |
|------|------|----------|----------|
| Stage 0 | 选题与自动采集 | `Master action plan · MD`、`podcast-news-aggregator/data/report_*.md`、`ai_poadcast_main/daily_review.txt` | `podcast-news-aggregator`、`collect_rss_feeds.py`、`daily_news_workflow.sh`、RSSHub |
| Stage 1 | 原文导入与存档 | `source_archive/YYYY-MM-DD/<slug>.md`、`source_archive/_index.json` | `import_raw_story.py`、`process_queue.py` |
| Stage 2 | 生成要点卡片 + QA | `ai_poadcast_main/news_queue_with_summaries.json`、`ai_poadcast_main/daily_review_cn.md`、`要点卡片/..._card_final.md` | `daily_workflow.py` (`--extract`)、Prompt 2/3、QA Checklist |
| Stage 3 | 撰写播客脚本 | `脚本输出/YYYY-MM-DD/episode_<date>_v*.md` | `prepare_stage3_prompt.py`、`generate_stage3_script.py`、朗读校对 |
| Stage 4 | QA 复核与结论 | `ai_poadcast_main/stage3_reviews/..._review.md` | QA Checklist、Workflow Board |
| Stage 5 | 音频合成与归档 | `audio_exports/YYYY/episode_<date>_<voice>.ext` | `tts_xunfei.py`、`tts_volcengine.py`、`say`、Edge TTS |
| Stage 6 | 发布与回溯 | 看板更新、复盘记录 | `workflow_status_board.md`、Master Action Plan |

---

## 4. 操作流程

### 4.0 Stage 0 — 新闻采集（可选）
1. 运行一键脚本获取最新候选新闻：
   ```bash
   bash ai_poadcast_main/daily_news_workflow.sh
   ```
   - 调用 `collect_rss_feeds.py` 拉取 RSS；
   - 自动导入优先级 ≥9 的新闻（默认 3 条，若需关闭在 Python 入口中使用 `--auto-import 0`）；
   - 可选整合 `podcast-news-aggregator` 演示输出（默认开启，可通过 `--no-demo` 关闭）；
   - 输出 `ai_poadcast_main/daily_review.txt`，便于快速浏览。
2. 需要更细的筛选时，可单独执行 `collect_rss_feeds.py` 或使用 `podcast-news-aggregator/` 目录下的采集 Demo。

### 4.1 Stage 1 — 原文导入
1. 复制新闻原文（剪贴板或文件）。
2. 执行脚本：
   ```bash
   pbpaste | python ai_poadcast_main/import_raw_story.py        --title "Monash to open 2032 Malaysia campus with backing from Australian PM"        --source "The PIE News"        --url https://thepienews.com/monash-to-open-2032-malaysia-campus-with-backing-from-australian-pm-albanese/        --published-date 2025-10-28        --fetch --store-html
   ```
3. 脚本自动完成：
   - 原文存档：`source_archive/YYYY-MM-DD/<slug>.md`（含元数据、词数、是否抓取）。
   - 索引更新：`source_archive/_index.json`。
   - （若指定 `--fetch --store-html`）下载 HTML：`source_archive/YYYY-MM-DD/<slug>.html`。

> **提示：** 若 URL 已存在或原文字数 <80，将收到警告。可以使用 `search_source_index.py` 快速查询：
> ```bash
> python ai_poadcast_main/search_source_index.py --source "The PIE" --limit 5
> ```

### 4.2 Stage 2 — 要点卡片与校验
1. 运行一键流水线（默认包含 Stage 0/2/3）：
   ```bash
   python ai_poadcast_main/daily_workflow.py
   ```
   - 生成 `news_queue_with_summaries.json`、`daily_review_cn.md`、`daily_review.txt`；
   - 在终端确认导入的新闻，脚本会自动写入 `source_archive/`；
   - 自动生成 Stage 3 Prompt 与脚本初稿。
   如仅需部分阶段，可使用 `--collect/--extract/--review/--stage3` 等参数分别执行。
2. 在 `daily_review_cn.md` 中逐条审阅要点，必要时修改 `news_queue_with_summaries.json` 的 `chinese_summary` 字段或补充备注。需要归档终稿时，可将定稿写入 `要点卡片/YYYY-MM-DD/<slug>_card_final.md`。
3. 使用 **附录 B**、**附录 C** 完成术语/数字校验，记录在 `stage2_reviews/YYYY-MM-DD/` 下的 QA 文档，并在 QA Checklist Stage 2 区域勾选完成项。

### 4.3 Stage 3 — 脚本生成
1. `daily_workflow.py` 默认已生成 `stage3_inputs/<date>/episode_<date>_prompt.md` 与 `脚本输出/<date>/episode_<date>_v1.md`。如需重新汇总或自定义条目，可选择：
   ```bash
   python ai_poadcast_main/prepare_stage3_prompt.py --date 2025-10-31 --count 4 --min-priority 9 --select 1 3 5
   python ai_poadcast_main/generate_stage3_script.py --prompt ai_poadcast_main/stage3_inputs/2025-10-31/episode_2025-10-31_prompt.md --overwrite
   ```
2. 如需修订脚本，继续输出 `episode_<date>_v2.md`、`_v3.md`，最终确认版本命名为 `episode_<date>_final.md`。
3. 朗读全文，调整拗口句，更新 QA Checklist Stage 3 栏位。

### 4.4 Stage 4 — QA 与审核记录
1. 复制 `qa_checklist_template.md` 到 `ai_poadcast_main/stage3_reviews/YYYY-MM-DD/episode_<date>_review.md`。
2. 逐项核对事实、风险语言、朗读顺畅度，记录问题与修复。
3. 在 `workflow_status_board.md` 或 Notion/飞书看板更新状态，标记责任人、截止时间与结论。

### 4.5 Stage 5 — 音频合成

**推荐方案（按场景选择）：**

**方案A：中文播客（首选）- 讯飞TTS**
```bash
python tts_xunfei.py \
    --text-file 脚本输出/2025-10-29/episode_2025-10-29_final.md \
    --output audio_exports/2025/episode_2025-10-29_xiaoyan.mp3 \
    --voice xiaoyan \
    --speed 55 \
    --segment-size 600 \
    --max-retries 5 \
    --retry-delay 1.5
```
- 运行前在 shell 中导出 `XUNFEI_APP_ID`/`XUNFEI_API_SECRET`/`XUNFEI_API_KEY`（可通过 `set -a; 
 .env; set +a` 载入）。  
- `--segment-size` 控制单次推送的字数，遇到 10222 超时时适当调小；`--max-retries` 与 `--retry-delay` 会自动退避重试。  
- 默认输出 MP3（lame），如需 WAV 可在脚本内调整 `audio_format`。

**优点：**
- 中文音质行业第一
- 500次/天免费额度（日更绰绰有余）
- 100+发音人可选
- 自动处理长文本分段

**方案B：语音克隆/多轨 - 火山引擎**
```bash
python tts_volcengine.py \
    --text-file 脚本输出/2025-10-29/episode_2025-10-29_final.md \
    --output audio_exports/2025/episode_2025-10-29_clone.wav
```

**方案C：快速测试 - macOS say**
```bash
say -v Ting-Ting -f script.txt -o script.aiff
afconvert -f m4af -d aac script.aiff audio.m4a
```
- 所有音频放置于 `audio_exports/YYYY/`，命名 `episode_<date>_<voice>.ext`。
- 将最终采用的音频文件名写入 QA 模板“阶段 4”区块。
- 参考 **音频方案对照表** 选择合适工具。
- `script_chunks/` 保存长脚本文本的分段稿，`audio_parts/` 缓存分段合成的 WAV/MP3，最终只归档 `audio_exports/` 中的成品文件。

### 4.6 Stage 6 — 发布与复盘
1. 把音频上传到发布渠道，并在看板中更新为 Done。
2. 在 `Master action plan · MD` 中记录亮点与不足。
3. 如有结构性问题（如 Prompt 冗长、QA 漏洞），在 README 或模板中及时修订。

---

## 5. 工具箱

### 5.1 `import_raw_story.py`
- 支持剪贴板 / 文件 / `--fetch` 网页抓取。
- 自动标准化 URL、去重、统计词数、存档 HTML。
- 常用参数：
  | 参数 | 说明 |
  |------|------|
  | `--title` `--url` *(必填)* | 新闻标题与原始链接 |
  | `--source` | 媒体名称，便于索引检索 |
  | `--published-date` | `YYYY-MM-DD`，缺失会收到提醒 |
  | `--tags` | 可多次填写的标签，自动去重 |
  | `--fetch` `--store-html` | 自动抓取正文及保存 HTML |
  | `--force` | 允许重复导入同一 URL |

### 5.2 `search_source_index.py`
- 支持按日期、来源、标题、标签、URL 精确过滤。
- 常见用法：
  ```bash
  # 查看最新一条
  python ai_poadcast_main/search_source_index.py --latest
  
  # 按标签检索
  python ai_poadcast_main/search_source_index.py --tag policy --tag visa --limit 10
  ```

### 5.3 `tts_volcengine.py`
- 调用火山引擎 MaaS TTS v3 双向 WebSocket。
- 主要环境变量见 README 第 5 节；支持语音克隆、多轨混音。
- 若返回 `ErrorCode: 45000030`，表示 voice_type 未获授权。

### 5.4 `tts_xunfei.py`
- 调用讯飞在线语音合成 WebSocket API。
- 主要环境变量：`XUNFEI_APP_ID`、`XUNFEI_API_SECRET`、`XUNFEI_API_KEY`。
- 支持100+发音人，中文音质优秀，每日500次免费额度。
- 长文本自动分段合成，可通过 `--segment-size` 指定每段最大字符数。
- 支持 `--max-retries`、`--retry-delay` 调节重试策略，避免 10222 超时导致空文件。
- 常用命令：
```bash
  python tts_xunfei.py \
      --text-file 脚本输出/2025-10-29/episode_2025-10-29_final.md \
      --output audio_exports/2025/episode_2025-10-29_xiaoyan.mp3 \
      --voice xiaoyan \
      --speed 55 \
      --segment-size 600 \
      --max-retries 5 \
      --retry-delay 1.5
```
- 若返回 `10222 DeadlineExceeded`，先减小 `--segment-size`、提高重试参数，并确认网络/账号限流状态。
- 若返回错误码 `10163`，表示 IP 未加白名单，需在讯飞控制台添加。
- 若返回错误码 `10907`，表示日调用量超限（免费版500次/天）。

**发音人推荐：**
| 发音人 | 音色 | 适用场景 |
|--------|------|----------|
| xiaoyan | 普通话女声（温和） | 日常播报 |
| aisjiuxu | 四川话女声 | 地方特色 |
| xiaoyu | 普通话男声（沉稳） | 严肃新闻 |
| vimary | 英文女声 | 双语播客 |

完整列表：https://www.xfyun.cn/doc/tts/online_tts/API.html#%E5%8F%91%E9%9F%B3%E4%BA%BA%E5%88%97%E8%A1%A8

### 5.5 macOS `say` + `afconvert`
- 离线命令：
  ```bash
  say -v Ting-Ting -f script.txt -o script.aiff
  afconvert -f m4af -d aac script.aiff audio_exports/2025/episode_2025-10-29_Ting-Ting.m4a
  ```
- 语速、停顿可通过 `[[rate 190]]`、`[[slnc 200]]` 控制。

### 5.6 Edge TTS（可选）
- 安装：`pip install edge-tts`
- 示例：
  ```bash
  edge-tts --voice zh-CN-XiaoxiaoNeural       --text "$(cat script.txt)"       --output audio_exports/2025/episode_2025-xx-xx_edge.mp3
  ```

### 5.7 自动采集与批处理工具
- `collect_rss_feeds.py`：批量抓取配置在 `feeds_config.py` 中的 RSS 源，生成候选新闻列表。
- `scrape_exam_sites.py`：抓取考试/排名类官网动态，补足 RSS 难以覆盖的渠道。
- `batch_extract.py`：将 `source_archive/` 里的原文批量转为 Stage 2 Prompt 输入，支持按日期过滤。
- `process_queue.py` + `daily_news_workflow.sh`：示范如何用队列批量跑 Stage 1-3，结合 `script_chunks/`、`audio_parts/` 输出百分百可复现的流水线。
- `podcast-news-aggregator/`：独立的自动采集 Demo，可用 `scripts/integrated_demo.py` 快速产出 `data/report_*.md`，为 Stage 0 选题提供素材。
- `check_usage.sh`：查询当前 TTS 服务的调用额度，配合 `tts_xunfei.py`/火山 MaaS 监控成本。

---

## 6. QA 与看板
- 质检模板：`ai_poadcast_main/qa_checklist_template.md`（附录 E）。
- 看板示例：`ai_poadcast_main/workflow_status_board.md`（附录 F），可复制到 Notion/飞书维持状态同步。
- 建议每期 QA 结束后在 README 的音频方案表中勾选使用路径，并回写至 Master action plan。

---

## 7. 音频方案对照表

| 方案 | 依赖 | 适用场景 | 优点 | 注意事项 | 命名示例 |
|------|------|----------|------|----------|----------|
| macOS `say` + `afconvert` | macOS 自带命令 | 快速生成参考音频/中文播讲 | 离线、命令简单 | 声线有限，需手动控制停顿、语速 | `audio_exports/2025/episode_2025-10-29_Ting-Ting.m4a` |
| Volcengine `tts_volcengine.py` | Python 虚拟环境、火山凭证 | 正式播出音频 | 支持语音克隆、多轨混音、长文本流式 | 确保 voice_type 获授权，注意 4xx 错误码 | `audio_exports/2025/episode_2025-10-29_raw.wav` |
| Volcengine `generate_audio_openspeech.py` | 火山 AppID/Token、官方 demo | 批量拆段合成 | 自动拆分 + ffmpeg 拼接，可继承 Demo 能力 | 未获授权 voice_type 会返回 403；需安装 ffmpeg | `audio_exports/2025/episode_2025-xx-xx_special.m4a` |
| Edge TTS (`edge-tts`) | pip 包、外网 | 多语种/多声线测试 | Microsoft 神经音色丰富，支持 SSML | 长文本需拆段，速率受限 | `audio_exports/2025/episode_2025-xx-xx_edge.mp3` |
| 其他 SaaS（Azure、Google、ElevenLabs） | 对应 API Key、计费账户 | 情感化播报、克隆音色 | 自然度高，可调情绪/语速 | 注意版权、账单及密钥管理 | 自定义命名，保留平台标签 |
| 讯飞 `tts_xunfei.py` | Python 环境、讯飞凭证 | 中文播客正式版 | 中文音质行业领先，500次/日免费，发音人丰富 | 需IP白名单，长文本需分段，超限后付费 | `audio_exports/2025/episode_2025-10-29_xiaoyan.mp3` |

> 音频统一存放 `audio_exports/YYYY/`，命名规则：`episode_<日期>_<描述/声线>.<格式>`，并在 `episode_<date>_review.md` 标注最终采用的文件。

---

## 附录

### 附录 A：Prompt 1（要点卡片生成）
```markdown
你是国际教育播客的内容编辑。请将以下英文新闻提取要点并翻译成中文。

**新闻原文：**
[粘贴英文新闻全文]

**新闻来源：** [URL]
**发布日期：** [YYYY-MM-DD]

---

### 📋 要点卡片

**【中文标题】**
[一句话概括，15字以内]

**【核心要点 - 5W1H】**
- **What（什么事）：** [一句话说清楚核心事件]
- **Who（影响谁）：** [具体人群，如"2025年秋季入学的英国硕士申请者"]
- **When（时间）：** [生效/申请/截止日期，必须明确]
- **Where（地区）：** [具体国家/院校，避免说"海外"]
- **Why（为什么）：** [政策背景或原因]
- **How（怎么办）：** [申请流程/注意事项/应对建议]

**【关键数据】**（如有）
- 数字1：[金额/比例/人数，务必来自原文]
- 数字2：...

**【时间节点】**（如有）
- 发布日期：2025-01-15
- 生效日期：2025-03-01
- 申请截止：2025-02-28

**【原文关键句】**（用于后续核验）
> "[摘录原文中最关键的一句话，英文，<50词]"

**【来源评级】**
- 来源类型：☑ 官方一手 / ☐ 权威二手 / ☐ 一般媒体
- 可信度：☑ 高 / ☐ 中 / ☐ 低
- 建议：☑ 可直接使用 / ☐ 需交叉验证

---

**特殊要求：**
1. 所有专有名词必须标注：如"University of Cambridge（剑桥大学）"
2. 所有日期统一格式：2025年1月15日
3. 所有金额标注汇率：如"£20,000（约¥18万人民币，按2025年1月汇率）"
4. 不要使用"据报道"等模糊表述，必须写"据XX官网/XX机构"
5. 如果某个要素不明确，标注"[原文未提及]"
```

### 附录 B：Prompt 2（术语校验）
```markdown
请检查以下中文文本中的专有名词翻译是否准确，并按术语库进行修正：

**文本：**
[粘贴AI翻译的中文卡片]

**术语库：**
{
  "院校": {
    "University of Cambridge": "剑桥大学",
    "MIT": "麻省理工学院（MIT）",
    "Oxford": "牛津大学"
  },
  "考试": {
    "IELTS": "雅思（IELTS）",
    "TOEFL": "托福（TOEFL）",
    "GRE": "美国研究生入学考试（GRE）",
    "GMAT": "管理学研究生入学考试（GMAT）"
  },
  "签证": {
    "PSW": "毕业生工作签证（PSW，Post-Study Work）",
    "Tier 4": "英国学生签证（第四层级）",
    "F-1": "美国F-1学生签证",
    "Graduate visa": "英国毕业生签证"
  },
  "机构": {
    "Home Office": "英国内政部（Home Office）",
    "USCIS": "美国移民局（USCIS）",
    "UKVI": "英国签证与移民局（UKVI）"
  }
}

**输出格式：**
### 检查结果
- ✅ 正确：[列出翻译正确的术语]
- ❌ 需修正：
  - 原文："University of Cambridge" → 误译："剑桥郡大学" → 正确："剑桥大学"

### 修正后文本
[输出修正后的完整文本]
```

### 附录 C：Prompt 3（数字与日期核对）
```markdown
请严格校验以下中文卡片中的数字、日期、金额是否与英文原文一致：

**英文原文：**
[粘贴原文]

**中文卡片：**
[粘贴要点卡片终稿]

**检查事项：**
- 金额单位、汇率换算是否正确（如 £ → ¥、$ → ¥）。
- 日期是否与原文一致，格式统一为 "2025年1月15日"。
- 比例、增长率、人数等是否逐一对应。
- 若原文未提到数字，请提示"中文卡片包含额外数字：..."。

**输出格式：**
- ✅ 一致：...
- ❌ 异常：...
  - 修正建议：...
```

### 附录 D：Stage 3 脚本 Prompt
```markdown
你是《异乡早咖啡》播客的脚本作者大刘。现在需要将以下新闻要点编写成一期8分钟的播客脚本。

---

## 📰 本期新闻要点

### 新闻1：[类别-政策]
[粘贴完整的要点卡片1]

### 新闻2：[类别-院校]
[粘贴完整的要点卡片2]

### 新闻3：[类别-考试]
[粘贴完整的要点卡片3]

### （可选）新闻4：[类别-其他/补充]
[如无补充新闻，可留空]

---

## ✍️ 脚本要求

### 【基本原则】
1. **事实准确**：每个具体信息必须来自要点卡片，不得臆造
2. **来源标注**：关键信息后必须加"据XX官网/机构报道"
3. **口语化**：使用对话式语言，避免书面语
4. **听众视角**：多用"如果你正在准备申请..."、"这对你意味着..."
5. **时间感**：明确时间节点，如"从3月1日起"、"在2月底前"
6. **时长控制**：全文1500-1800字，口播时长≤8分钟，整期不超过5条新闻/数据点

### 【语言风格】
- ✅ 使用："这周最大的新闻是..."、"值得注意的是..."、"简单来说..."
- ❌ 避免："根据相关报道"、"有关部门表示"、"据悉"等模糊表述
- ✅ 数字重复：重要数字说2遍，如"延长1年，也就是从2年变成3年"
- ❌ 避免：连续3句以上都是数据，需要穿插解读

### 【结构要求】
每条新闻包含：
1. 引入（1句话吸引注意）
2. 核心事实（5W1H）
3. 背景解读（为什么重要）
4. 实用建议（听众怎么办）
5. 过渡语（连接下一条新闻）

---

## 📝 脚本模板（直接填充）

### 【开场白】(20秒 / 70-80字)

> 大家好，欢迎收听《异乡早咖啡》，我是[大刘]。今天是2025年[X月X日]，我们将在8分钟内快速带你盘点国际教育领域的重磅动态。
> 
> 本期我们聚焦三个关键词：[话题1]、[话题2]、[话题3]。如果你正在关注国际教育行业全球动态，记得点击订阅。
> 
> 那我们马上开始第一条新闻。

---

### 【主体内容-新闻1】(2分30秒 / 480-520字)

**[分节标题]** 🔴 重磅：[新闻标题]

> 本周最值得关注的，是据[来源]在[日期]发布的[一句话核心事件]。
>
> 换句话说，[用口语解释核心变化]。对[受众群体]来说，有三个重点要记住：
>
> **重点一**  
> [关键事实或数据]，意味着[影响解读]。
>
> **重点二**  
> [第二个事实或举措]，提醒你[风险提示或限制]。
>
> **重点三**  
> [第三个事实]，时间节点是[明确deadline/生效日期]。
>
> 完整细节记得看节目简介里的官方链接。接下来我们转到第二条消息。

---

### 【主体内容-新闻2】(1分45秒 / 330-370字)

**[分节标题]** 🎓 院校/市场：[新闻标题]

> 据[来源]报道，[一句话核心事件]。
>
> 背后折射出[背景/趋势]，两个数字特别关键：[数据1]和[数据2]，它们说明[解读]。
>
> 对申请者最直接的影响是[具体影响]。建议你：
> - [建议或检查点1]
> - [建议或检查点2]
>
> [顺滑过渡句：例如“说完院校动向，我们看看考试端的变化。”]

---

### 【主体内容-新闻3】(1分45秒 / 330-370字)

**[分节标题]** 📝 考试/数据：[新闻标题]

> 如果你正在关注[考试名称或数据主题]，这一则更新要收好。据[来源]披露，[一句话核心事件]。
>
> 具体变化包括：
> - [变化1]
> - [变化2]
> - [可选变化3]
>
> 备考或申请策略上，你需要：
> 1. [建议1]
> 2. [建议2]
>
> 生效时间是[具体日期]，别忘了核对原文链接。

---

### 【数据速览】(50秒 / 150-170字)

> 快速扫三组数字：
> - **[指标1]**：[数字/区间]，代表[一句话解读]。
> - **[指标2]**：[数字/区间]，说明[一句话解读]。
> - **重要日期**：[X月X日]是[事件]的截止/开放时间，请设置提醒。
>
> 以上数据均来自[来源列表]，详情链接见节目简介。

---

### 【结尾】(40秒 / 120-140字)

> 以上就是本期《异乡早咖啡》。三条新闻分别是：[一句话串联三条新闻]。再强调一次，[最重要的deadline或数字]。
>
> 如果你觉得本期有用，欢迎分享给行业的朋友，也欢迎在[互动渠道]告诉我你想了解的话题。
>
> 我是[大刘]，感谢收听，我们下期见。

---

### 风格调优提示
- 若脚本过于正式，可使用“请降低语体，增加互动感”类指令对输出做二次润色。
- 保持每段不超过 4 句，数字后加一次解释（"也就是..."）。
- 重要名词首次出现带中文括注。

```

（见 `ai_poadcast_main/qa_checklist_template.md`，可直接复制使用。）

### 附录 F：工作流看板示例
（见 `ai_poadcast_main/workflow_status_board.md`，可同步到协作平台。）

---

如流程或模板有更新，请同步修改本 README，并在 `workflow_status_board.md` 和 `qa_checklist_template.md` 中保持一致，确保团队始终使用最新版本。
